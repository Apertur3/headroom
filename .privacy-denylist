# scripts/privacy-sweep.sh: one regex per line, checked against every tracked
# file and the packed npm tarball. A leading (?i) makes that one line
# case-insensitive; a line starting with # is a comment.
#
# This tracked file holds only generic patterns. Names of people, machines or
# private hosts belong in an untracked local list: the file named by the
# PRIVACY_DENYLIST environment variable, or ~/.config/headroom-privacy-denylist.
# The sweep reads both.
Co-Authored-By
Claude-Session
