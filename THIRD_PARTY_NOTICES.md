# Third-party notices

| Component | Licence | Use in Headroom |
|---|---|---|
| CodexBar / CodexBarCLI (steipete) | MIT | Pinned sensing engine, run as a child process; JSON schema adapted in `src/engine/` |
| Orca claude-fetcher (Plotter fork, `~/Projects/plotter/desktop/src/main/rate-limits/`) | see repo | Reference for the native Claude adapter (Keychain service naming, usage endpoint contract) |
| the fleet pace machine pace machine (`~/Projects/plotter/control/`) | own code | Pace-state logic ported into `src/policy/` |

Copyleft sources are never read; behaviour-only intel is allowed.
