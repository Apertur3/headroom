# Each entry names one credential location. Headroom never stores credentials here.
[[accounts]]
name = "codex-main"
vendor = "codex"
location = "~/.codex"
adapter = "native-ts"

[[accounts]]
name = "claude-main"
vendor = "claude"
location = "~/.claude"
adapter = "native-ts"

[[accounts]]
name = "claude-2"
vendor = "claude"
location = "~/.claude2"
adapter = "native-ts"

[[accounts]]
name = "antigravity"
vendor = "antigravity"
location = "~/.gemini/antigravity-cli"
adapter = "engine"

# Local pools are probed only; a wake command is reported when down and never run.
# [[accounts]]
# name = "gpu-box-vllm"
# kind = "local"
# base_url = "http://10.0.0.20:8000"
# adapter = "native"

# [[accounts]]
# name = "workstation-vllm"
# kind = "local"
# base_url = "http://10.0.0.20:8000"
# adapter = "native"
